apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "memsidecar.fullname" . }}-config
  labels:
    {{- include "memsidecar.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- toYaml .Values.config | nindent 4 }}
