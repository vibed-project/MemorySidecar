"""Optional framework integrations for mindd.

Each submodule targets a specific agent framework and depends on that
framework being installed (an extra, e.g. ``pip install mindd[langgraph]``
or ``mindd[crewai]``). Importing :mod:`mindd` itself never pulls
these in.
"""
